name: Deploy

on:
  push:
    branches: [dev, qa, main]
  pull_request:
    branches: [dev, main]

# Minimal permissions — only what each job needs
permissions:
  id-token: write   # Required for OIDC token
  contents: read

env:
  AWS_REGION: us-east-1
  TF_DIR: infrastructure

jobs:
  # ─────────────────────────────────────────────
  # Job 0: Resolve GitHub Environment name + AWS config
  # ─────────────────────────────────────────────
  set-environment:
    name: Set Environment
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.set-env.outputs.environment }}
      account_id: ${{ steps.aws-config.outputs.account_id }}
      deployer_role: ${{ steps.aws-config.outputs.deployer_role }}
      state_role: ${{ steps.aws-config.outputs.state_role }}
      cloudfront_aliases_json: ${{ steps.aws-config.outputs.cloudfront_aliases_json }}
    steps:
      - id: set-env
        name: Map branch → environment
        run: |
          case "${{ github.ref_name }}" in
            dev)  echo "environment=dev"  >> $GITHUB_OUTPUT ;;
            qa)   echo "environment=qa"   >> $GITHUB_OUTPUT ;;
            main) echo "environment=prod" >> $GITHUB_OUTPUT ;;
            *)
              echo "Unknown branch: ${{ github.ref_name }}" >&2
              exit 1
              ;;
          esac

      - name: Set AWS config
        id: aws-config
        run: |
          case "${{ github.ref_name }}" in
            dev)
              echo "account_id=680666325340" >> $GITHUB_OUTPUT
              echo "deployer_role=arn:aws:iam::680666325340:role/github-actions-deployer" >> $GITHUB_OUTPUT
              echo 'cloudfront_aliases_json=[]' >> $GITHUB_OUTPUT
              ;;
            qa)
              echo "account_id=944884338542" >> $GITHUB_OUTPUT
              echo "deployer_role=arn:aws:iam::944884338542:role/github-actions-deployer" >> $GITHUB_OUTPUT
              echo 'cloudfront_aliases_json=[]' >> $GITHUB_OUTPUT
              ;;
            main)
              echo "account_id=718417033936" >> $GITHUB_OUTPUT
              echo "deployer_role=arn:aws:iam::718417033936:role/github-actions-deployer" >> $GITHUB_OUTPUT
              echo 'cloudfront_aliases_json=["juicetech.io","www.juicetech.io","juicetech.io","www.juicetech.io"]' >> $GITHUB_OUTPUT
              ;;
          esac
          echo "state_role=arn:aws:iam::389813258758:role/github-actions-terraform-state" >> $GITHUB_OUTPUT

  # ─────────────────────────────────────────────
  # Job 1: Provision infrastructure with Terraform
  # ─────────────────────────────────────────────
  terraform-apply:
    name: Terraform Apply
    if: github.event_name == "push"
    runs-on: ubuntu-latest
    needs: set-environment
    environment: ${{ needs.set-environment.outputs.environment }}
    outputs:
      cloudfront_distribution_id: ${{ steps.tf-output.outputs.cloudfront_distribution_id }}
      s3_bucket_name: ${{ steps.tf-output.outputs.s3_bucket_name }}
      cloudfront_domain_name: ${{ steps.tf-output.outputs.cloudfront_domain_name }}
      rum_app_monitor_id: ${{ steps.tf-output.outputs.rum_app_monitor_id }}
      rum_identity_pool_id: ${{ steps.tf-output.outputs.rum_identity_pool_id }}
      rum_guest_role_arn: ${{ steps.tf-output.outputs.rum_guest_role_arn }}
      rum_region: ${{ steps.tf-output.outputs.rum_region }}
      contact_api_url: ${{ steps.tf-output.outputs.contact_api_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Configure AWS credentials (Terraform state)
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: ${{ needs.set-environment.outputs.state_role }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd
        with:
          terraform_version: "~1.7"
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform init \
            -backend-config="key=company-homepage/${{ needs.set-environment.outputs.environment }}/terraform.tfstate"

      # One-time remediation: clear any stale SES state entries that may exist
      # without a corresponding AWS resource (state drift). Idempotent — safe
      # to run every deploy; terraform state rm exits 0 even if the resource
      # is already absent from state.
      - name: Remove stale SES state (idempotent)
        working-directory: ${{ env.TF_DIR }}
        run: |
          terraform state rm aws_ses_domain_identity.contact || true
          terraform state rm aws_ses_domain_dkim.contact     || true
          terraform state rm terraform_data.ses_domain_key   || true

      - name: Terraform Plan
        working-directory: ${{ env.TF_DIR }}
        env:
          TF_VAR_contact_from_email: ${{ vars.CONTACT_FROM_EMAIL }}
          TF_VAR_contact_to_email: ${{ vars.CONTACT_TO_EMAIL }}
          TF_VAR_contact_email_domain: ${{ vars.CONTACT_EMAIL_DOMAIN }}
        run: |
          terraform plan \
            -var="environment=${{ needs.set-environment.outputs.environment }}" \
            -var="deployer_role_arn=${{ needs.set-environment.outputs.deployer_role }}" \
            -var='cloudfront_aliases=${{ needs.set-environment.outputs.cloudfront_aliases_json }}' \
            -out=tfplan

      - name: Skip apply on pull requests
        if: github.event_name == "pull_request"
        run: echo "PR run complete: terraform plan only, no apply."

      - name: Terraform Apply
        if: github.event_name == "push"
        working-directory: ${{ env.TF_DIR }}
        run: terraform apply -auto-approve tfplan

      - name: Get Terraform Outputs
        id: tf-output
        working-directory: ${{ env.TF_DIR }}
        run: |
          CF_ID=$(terraform output -raw cloudfront_distribution_id)
          BUCKET=$(terraform output -raw s3_bucket_name)
          CF_DOMAIN=$(terraform output -raw cloudfront_domain_name)
          RUM_MONITOR_ID=$(terraform output -raw rum_app_monitor_id)
          RUM_IDENTITY_POOL_ID=$(terraform output -raw rum_identity_pool_id)
          RUM_GUEST_ROLE_ARN=$(terraform output -raw rum_guest_role_arn)
          RUM_REGION=$(terraform output -raw rum_region)
          CONTACT_API_URL=$(terraform output -raw contact_api_url)
          echo "cloudfront_distribution_id=$CF_ID" >> "$GITHUB_OUTPUT"
          echo "s3_bucket_name=$BUCKET" >> "$GITHUB_OUTPUT"
          echo "cloudfront_domain_name=$CF_DOMAIN" >> "$GITHUB_OUTPUT"
          echo "rum_app_monitor_id=$RUM_MONITOR_ID" >> "$GITHUB_OUTPUT"
          echo "rum_identity_pool_id=$RUM_IDENTITY_POOL_ID" >> "$GITHUB_OUTPUT"
          echo "rum_guest_role_arn=$RUM_GUEST_ROLE_ARN" >> "$GITHUB_OUTPUT"
          echo "rum_region=$RUM_REGION" >> "$GITHUB_OUTPUT"
          echo "contact_api_url=$CONTACT_API_URL" >> "$GITHUB_OUTPUT"
          echo "::notice title=CloudFront URL::$CF_DOMAIN"

  # ─────────────────────────────────────────────
  # Job 2: Build Next.js and deploy to S3
  # ─────────────────────────────────────────────
  build-and-deploy:
    name: Build & Deploy
    if: github.event_name == "push"
    runs-on: ubuntu-latest
    needs: [set-environment, terraform-apply]
    environment: ${{ needs.set-environment.outputs.environment }}
    env:
      CF_DISTRIBUTION_ID: ${{ needs.terraform-apply.outputs.cloudfront_distribution_id }}
      S3_BUCKET: ${{ needs.terraform-apply.outputs.s3_bucket_name }}
      IAM_ROLE: ${{ needs.set-environment.outputs.deployer_role }}
      NEXT_PUBLIC_AWS_RUM_APP_MONITOR_ID: ${{ needs.terraform-apply.outputs.rum_app_monitor_id }}
      NEXT_PUBLIC_AWS_RUM_IDENTITY_POOL_ID: ${{ needs.terraform-apply.outputs.rum_identity_pool_id }}
      NEXT_PUBLIC_AWS_RUM_GUEST_ROLE_ARN: ${{ needs.terraform-apply.outputs.rum_guest_role_arn }}
      NEXT_PUBLIC_AWS_RUM_REGION: ${{ needs.terraform-apply.outputs.rum_region }}
      NEXT_PUBLIC_APP_VERSION: ${{ github.sha }}
      NEXT_PUBLIC_CONTACT_API_URL: ${{ needs.terraform-apply.outputs.contact_api_url }}

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Setup Node.js 20
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Audit dependencies
        run: npm audit --audit-level=high

      - name: Build static export
        run: npm run build

      - name: Configure AWS credentials (Deploy)
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: ${{ env.IAM_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync static assets to S3
        run: |
          aws s3 sync ./out s3://${{ env.S3_BUCKET }} \
            --delete \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "*.html" \
            --exclude "*.json" \
            --exclude "*.txt"

      - name: Sync HTML and manifests to S3
        run: |
          aws s3 sync ./out s3://${{ env.S3_BUCKET }} \
            --cache-control "public, max-age=0, must-revalidate" \
            --include "*.html" \
            --include "*.json" \
            --include "*.txt"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CF_DISTRIBUTION_ID }} \
            --paths "/*"

      - name: Print deployment URL
        run: |
          echo "✅ Deployed to: ${{ needs.terraform-apply.outputs.cloudfront_domain_name }}"
