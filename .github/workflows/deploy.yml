name: Deploy — Dev

on:
  push:
    branches: [dev]

# Minimal permissions — only what each job needs
permissions:
  id-token: write   # Required for OIDC token
  contents: read

env:
  AWS_REGION: us-east-1
  AWS_ACCOUNT_ID: "680666325340"
  IAM_ROLE: arn:aws:iam::680666325340:role/github-actions-deployer
  TF_DIR: infrastructure
  # State bucket is in a separate account; use TF_STATE_ROLE for Terraform operations
  TF_STATE_ROLE: arn:aws:iam::389813258758:role/github-actions-terraform-state

jobs:
  # ─────────────────────────────────────────────
  # Job 1: Provision infrastructure with Terraform
  # ─────────────────────────────────────────────
  terraform-apply:
    name: Terraform Apply
    runs-on: ubuntu-latest
    outputs:
      cloudfront_distribution_id: ${{ steps.tf-output.outputs.cloudfront_distribution_id }}
      s3_bucket_name: ${{ steps.tf-output.outputs.s3_bucket_name }}
      cloudfront_domain_name: ${{ steps.tf-output.outputs.cloudfront_domain_name }}

    steps:
      - name: Checkout
        # actions/checkout v4.2.2
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Configure AWS credentials (Terraform state)
        # aws-actions/configure-aws-credentials v4.0.2
        # Use TF_STATE_ROLE (account 389813258758) — has access to the Terraform state bucket
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: ${{ env.TF_STATE_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        # hashicorp/setup-terraform v3.1.2
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd
        with:
          terraform_version: "~1.7"
          terraform_wrapper: false

      - name: Terraform Init
        working-directory: ${{ env.TF_DIR }}
        run: terraform init

      - name: Terraform Plan
        working-directory: ${{ env.TF_DIR }}
        run: terraform plan -var="deployer_role_arn=${{ env.IAM_ROLE }}" -out=tfplan

      - name: Terraform Apply
        working-directory: ${{ env.TF_DIR }}
        run: terraform apply -auto-approve -var="deployer_role_arn=${{ env.IAM_ROLE }}"

      - name: Get Terraform Outputs
        id: tf-output
        working-directory: ${{ env.TF_DIR }}
        run: |
          CF_ID=$(terraform output -raw cloudfront_distribution_id)
          BUCKET=$(terraform output -raw s3_bucket_name)
          CF_DOMAIN=$(terraform output -raw cloudfront_domain_name)
          echo "cloudfront_distribution_id=$CF_ID" >> "$GITHUB_OUTPUT"
          echo "s3_bucket_name=$BUCKET" >> "$GITHUB_OUTPUT"
          echo "cloudfront_domain_name=$CF_DOMAIN" >> "$GITHUB_OUTPUT"
          echo "::notice title=CloudFront URL::$CF_DOMAIN"

  # ─────────────────────────────────────────────
  # Job 2: Build Next.js and deploy to S3
  # ─────────────────────────────────────────────
  build-and-deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest
    needs: terraform-apply
    env:
      CF_DISTRIBUTION_ID: ${{ needs.terraform-apply.outputs.cloudfront_distribution_id }}
      S3_BUCKET: ${{ needs.terraform-apply.outputs.s3_bucket_name }}

    steps:
      - name: Checkout
        # actions/checkout v4.2.2
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683

      - name: Setup Node.js 20
        # actions/setup-node v4.1.0
        uses: actions/setup-node@39370e3970a6d050c480ffad4ff0ed4d3fdee5af
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      # Sentinel M-4: Fail build on HIGH/CRITICAL vulnerabilities
      - name: Audit dependencies
        run: npm audit --audit-level=high

      - name: Build static export
        run: npm run build

      - name: Configure AWS credentials (Deploy)
        # aws-actions/configure-aws-credentials v4.0.2
        # Use IAM_ROLE (account 680666325340) — has access to the S3 bucket and CloudFront
        uses: aws-actions/configure-aws-credentials@e3dd6a429d7300a6a4c196c26e071d42e0343502
        with:
          role-to-assume: ${{ env.IAM_ROLE }}
          aws-region: ${{ env.AWS_REGION }}

      # Sync static assets (JS/CSS with content hashes) — long cache TTL
      - name: Sync static assets to S3
        run: |
          aws s3 sync ./out s3://${{ env.S3_BUCKET }} \
            --delete \
            --cache-control "public, max-age=31536000, immutable" \
            --exclude "*.html" \
            --exclude "*.json" \
            --exclude "*.txt"

      # Sync HTML and manifests — short TTL so updates propagate after CF invalidation
      - name: Sync HTML and manifests to S3
        run: |
          aws s3 sync ./out s3://${{ env.S3_BUCKET }} \
            --cache-control "public, max-age=0, must-revalidate" \
            --include "*.html" \
            --include "*.json" \
            --include "*.txt"

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ env.CF_DISTRIBUTION_ID }} \
            --paths "/*"

      - name: Print deployment URL
        run: |
          echo "✅ Deployed to: ${{ needs.terraform-apply.outputs.cloudfront_domain_name }}"
